import type { User, Role, Application, LoginActivity, Session, SSOProvider, EmailTemplate } from "./types";

// Mock/sample data removed. Expose empty initial collections so application code
// that imports these values continues to work without real or sample PII.

export const initialRoles: Role[] = [];
export const initialApplications: Application[] = [];
export const initialUsers: User[] = [];
export const initialLoginActivity: LoginActivity[] = [];
export const initialSessions: Session[] = [];
export const initialSSOProviders: SSOProvider[] = [];
export const initialEmailTemplates: EmailTemplate[] = [];